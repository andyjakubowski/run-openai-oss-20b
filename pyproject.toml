[project]
name = "temp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kernels>=0.9.0",
    "torch>=2.8.0",
    "transformers>=4.55.0",
]

[tool.uv]
python-preference="only-managed"
